// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyA70R_JP5UmFJDcKZrXjUQwWhSgqrwp1uM",
    authDomain: "clientpanelprod-49b1b.firebaseapp.com",
    databaseURL: "https://clientpanelprod-49b1b.firebaseio.com",
    projectId: "clientpanelprod-49b1b",
    storageBucket: "clientpanelprod-49b1b.appspot.com",
    messagingSenderId: "686221038120"

  }
  
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
